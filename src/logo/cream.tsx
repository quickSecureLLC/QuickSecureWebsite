/**
 * QuickSecure Logo - Cream Variant
 * Uses background color token for dark backgrounds
 */

import { Logo } from './Logo';

export function LogoCream() {
  return <Logo color="var(--qs-bg)" />;
}
